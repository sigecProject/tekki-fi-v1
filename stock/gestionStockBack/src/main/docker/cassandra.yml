version: '2'
services:
  gestionstockback-cassandra:
    image: cassandra:3.11.5
    # volumes:
    #     - ~/volumes/jhipster/gestionStockBack/cassandra/:/var/lib/cassandra/data
    ports:
      - 7000:7000
      - 7001:7001
      - 7199:7199
      - 9042:9042
      - 9160:9160
  gestionstockback-cassandra-migration:
    extends:
      file: cassandra-migration.yml
      service: gestionstockback-cassandra-migration
    environment:
      - CREATE_KEYSPACE_SCRIPT=create-keyspace.cql
